export BEADS_DIR="/Users/bobby/.local/share/git/bare/io.github/bdelanghe/.beads"

# Ensure repo-local Ruby version is active when direnv loads this env.
if command -v rbenv >/dev/null 2>&1; then
  eval "$(rbenv init -)"
fi
